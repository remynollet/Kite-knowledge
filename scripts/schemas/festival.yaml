# Schema for entity type: festival
type: festival
fields:
  id:
    type: string
    required: true
    description: "Kebab-case slug matching the filename exactly."
    notes: "No accents, no spaces, no uppercase. Must equal filename without .md extension."
  type:
    type: string
    required: true
    allowed_values: [festival]
    description: "Fixed value: festival"
  title:
    type: string
    required: true
    description: "Official festival name."
  date_range:
    type: string
    required: true
    description: "Active years or specific dates. E.g., '1987-present' or '2025-04-18'."
  geography:
    type: string
    required: true
    description: "Country or region where the festival takes place."
  category:
    type: string
    required: true
    description: "Festival type: international, local, competition, event, etc."
  tags:
    type: list
    required: true
    description: "At least one tag must match type 'festival'. Additional domain tags welcome."
  related:
    type: list
    required: true
    description: "Relative paths to related docs/ entities. Must use `../type/slug.md` for cross-directory links. May be empty list []."
  summary:
    type: string
    required: true
    description: "One-sentence summary shown in EntityHeader component."
  ai_status:
    type: string
    required: true
    allowed_values: [draft, in-review, validated]
    description: "Editorial lifecycle state."
  source_verified:
    type: bool
    required: true
    description: "True only when all sources have been checked by a human reviewer."
  reviewer:
    type: string
    required: false
    description: "GitHub username of reviewer. Null until reviewed."
    notes: "Must not be null in docs/ (enforced by validate_frontmatter.py extended checks)."
  review_date:
    type: string
    required: false
    description: "ISO date YYYY-MM-DD of last review. Null until reviewed."
  website:
    type: string
    required: false
    description: "Official festival website URL."
  social:
    type: list
    required: false
    description: "List of social media links (Facebook, Instagram, etc.)."
  affiliation:
    type: string
    required: false
    description: "Organizing body or main sponsor."
  coordinates:
    type: string
    required: false
    description: "GPS coordinates of the festival site."
  logo:
    type: string
    required: false
    description: "Path to the festival logo image."
  pinterest_board_id:
    type: string
    required: false
    description: "ID of the Pinterest board to embed (e.g., 'username/board-name')."
